logging:
  level:
    root: info

spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:localdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      username: sa
      password:
      driver-class-name: org.h2.Driver